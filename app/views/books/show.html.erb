<h2>Book was successfully created<h2>

  <strong>Title</strong>
  <p><%= @book.title %></p>
  
  <strong>Body</strong>
  <p><%= @book.body %></p>
  
  <%= link_to "Edit", edit_book_path(@book.id) %>
  |
　<%= link_to "Back", edit_book_path(@book.id) %>
